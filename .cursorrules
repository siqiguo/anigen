# AniGen - AI动画生成项目 User Rules

## 项目概述
AniGen是一个端到端的AI动画生成系统，实现从剧本到分镜脚本再到动画生成的全流程自动化。

## 核心工作流程
1. **剧本输入** (Script Input) - 接收用户提供的剧本文本
2. **剧本分析** (Script Analysis) - 解析剧本结构、角色、场景、对话等
3. **分镜生成** (Storyboard Generation) - 基于剧本自动生成分镜脚本
4. **动画生成** (Animation Generation) - 根据分镜脚本生成最终动画

## 技术栈偏好
- **语言**: Python (主要), TypeScript/JavaScript (前端)
- **AI框架**: 
  - 大语言模型: OpenAI API, Anthropic Claude API
  - 图像生成: Stable Diffusion, DALL-E
  - 视频生成: Runway, Pika, AnimateDiff
- **架构**: 模块化设计，微服务架构
- **数据格式**: JSON, YAML (配置文件)
- **API设计**: RESTful API

## 代码规范
- 使用类型提示 (Type Hints)
- 遵循 PEP 8 代码风格
- 函数和类必须有文档字符串
- 使用有意义的变量和函数名
- 每个模块保持单一职责原则
- 错误处理要完善，使用适当的异常类型

## 项目结构原则
- 模块化设计，每个功能模块独立
- 清晰的目录结构，按功能划分
- 配置文件与代码分离
- 日志记录要完善
- 支持配置文件和命令行参数

## 开发原则
- 优先使用现有的AI服务API，避免重复造轮子
- 代码要易于测试和维护
- 支持增量开发，每个模块可独立运行
- 提供清晰的错误信息和日志
- 考虑性能和成本优化

## 命名约定
- 模块名: 小写字母，下划线分隔 (snake_case)
- 类名: 大驼峰 (PascalCase)
- 函数/变量: 小写字母，下划线分隔 (snake_case)
- 常量: 全大写，下划线分隔 (UPPER_SNAKE_CASE)

## 文档要求
- 每个模块都要有README说明
- API接口要有文档字符串
- 复杂逻辑要有注释说明
- 配置文件要有示例和说明

## 测试要求
- 关键功能要有单元测试
- 集成测试覆盖主要工作流程
- 使用pytest作为测试框架

